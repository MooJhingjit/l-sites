
export type SearchParams = { [key: string]: string | string[] | undefined };

export type Params = {
  locale: string;
  routes: string[];
};

export type Meta = {
  title: string;
  description: string;
}
